"use client";

import ConstellationBackdrop from "./ConstellationBackdrop";

/**
 * PageBackdrop — thin alias that mounts the VINCENT constellation.
 * Kept under its legacy name so we don't have to touch every import.
 */
export default function PageBackdrop() {
  return <ConstellationBackdrop />;
}
